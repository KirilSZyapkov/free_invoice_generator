import SyncUser from "@/components/shared/SyncUser";

const HomePage = () => {
   return (
    <main className="p-6">
     <section>
      <SyncUser/>
     </section>
    </main>
  );
};

export default HomePage;
